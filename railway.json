{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "uv sync && uv run pip install -e .",
    "watchPatterns": [
      "**"
    ]
  },
  "deploy": {
    "startCommand": "uv run hypercorn main:app --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/healthz"
  }
}